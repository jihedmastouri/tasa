{
  "name": "tasa",
  "version": "1.0.0",
  "description": "Node in-memory datastore that runs in a separate worker.",
  "main": "index.js",
  "scripts": {
    "test": "tsc && test ./lib",
    "format:write": "prettier --write \"**/*.{ts,js,md}\" --ignore-path .gitignore --cache",
    "format:check": "prettier --check \"**/*.{ts,js,md}\" --ignore-path .gitignore  --cache",
    "build": "tsc"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.4"
  }
}
