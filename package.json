{
  "name": "tasa",
  "version": "0.0.0",
  "description": "Node in-memory datastore that runs in a separate worker.",
  "scripts": {
    "test": "pnpm clean && tsc && node --test ./lib/tests/*.test.js",
    "format:write": "biome format --write .",
    "format:check": "biome format .",
    "clean": "rm -rf ./lib",
    "build": "pnpm clean && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "@types/node": "^20.10.4"
  }
}
