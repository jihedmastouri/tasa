{
  "name": "tasa",
  "version": "1.0.0",
  "description": "Node in-memory datastore that runs in a separate worker.",
  "main": "index.js",
  "scripts": {
    "test": "tsc && test ./lib",
    "prettier-write": "prettier --write .",
    "prettier-check": "prettier --check .",
    "build": "tsc",
    "start": "tsc && node ./lib/index.run.js"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.6.5"
  }
}
